from setuptools import setup, find_packages

setup(
    name="GNR project",  # Replace with your project name
    version="0.1",
    packages=find_packages(),  # Automatically finds all packages (folders with __init__.py)
)
